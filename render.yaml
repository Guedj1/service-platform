services:
  - type: web
    name: servicesn-platform
    env: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /test
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
